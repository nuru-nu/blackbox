
- blackbox should stop when stop-sigterm is received (it currently ignores it)
- if any part is not working (e.g. clicker not found), program should start up
  and show error in web UI (instead of crashing / hanging)
